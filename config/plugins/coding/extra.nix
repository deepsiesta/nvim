{
  plugins.todo-comments = {
    enable = true;
    settings = {
      signs = true;
    };
  };

  plugins.sleuth = {
    enable = true;
  };

  plugins.nix.enable = true;
  plugins.nix-develop.enable = true;

  plugins.vimtex = {
    enable = true;
  };
}
